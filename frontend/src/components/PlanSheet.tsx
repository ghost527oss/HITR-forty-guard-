import { CHANGE_LEVELS, type ChangeLevel } from "../api";
import { X } from "lucide-react";
interface Props { pickedTemp?: number | null; open:boolean; onClose:()=>void; onConfirm:(level:ChangeLevel)=>void; hasPicked:boolean; loading?:boolean; }
export default function PlanSheet({open,onClose,onConfirm,hasPicked,loading}:Props){if(!open)return null;return <div className="absolute inset-0 z-40 flex items-end bg-black/30" onClick={onClose}><section className="w-full rounded-t-3xl bg-white p-5" onClick={e=>e.stopPropagation()}><div className="mb-3 flex justify-between"><h2 className="font-bold">Choose plan scale</h2><button onClick={onClose} aria-label="Close" className="text-gray-500">
        <X className="h-4 w-4" aria-hidden="true" />
      </button></div>{!hasPicked&&<p className="mb-3 text-sm text-amber-700">Select a point on the map first.</p>}<div className="space-y-2">{CHANGE_LEVELS.map(level=><button disabled={!hasPicked||loading} key={level.value} onClick={()=>onConfirm(level.value)} className="block w-full rounded-xl border p-3 text-left disabled:opacity-50"><b>{level.label}</b><span className="block text-xs text-gray-600">{level.desc}</span></button>)}</div></section></div>}
